#boys will be bugs, by cavetown, song submod by my-otter-self on reddit for MONIKA AFTER STORY

init 5 python in mas_bookmarks_derand:
    # ensure things get bookmarked and derandomed as usual.
    label_prefix_map["otter_song_"] = label_prefix_map["monika_"]

init 5 python:
    addEvent(
        Event(
            persistent._mas_songs_database,
            eventlabel="otter_song_boyswillbebugs",
            category=[mas_songs.TYPE_SHORT],
            prompt="Boys Will Be Bugs",
            random=True
        ),
        code="SNG"
    )

label otter_song_boyswillbebugs:
